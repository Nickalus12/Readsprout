name: audio_manager
description: "Reading Sprout Audio Manager — Deepgram TTS dev tool"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  just_audio: ^0.9.46
  just_audio_windows: ^0.2.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
